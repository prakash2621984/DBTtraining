Select 
ORDERID,
ORDERDATE,
SHIPDATE,
SHIPMODE,
CUSTOMERID,
CATEGORY,
PRODUCTNAME,
SUBCATEGORY
FROM
{{ ref(sample.ORDERS) }} AS O
LEFT JOIN
{{ ref(sample.PRODUCT) }} AS PRODUCT
ON
P.PRODUCTID=O.PRODUCTID

